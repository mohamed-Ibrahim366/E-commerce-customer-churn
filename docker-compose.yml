services:
  churn-ml:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - .:/app
    working_dir: /app
    environment:
      JUPYTER_PORT: 8888
